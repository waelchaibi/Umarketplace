NODE_ENV=production
PORT=5000

# Comma-separated list of allowed origins for CORS and Socket.io
# e.g. http://localhost:5173,https://your-frontend-domain.com
ALLOWED_ORIGINS=http://localhost:5173

# Database configuration (use managed MySQL in production)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=unique_marketplace

